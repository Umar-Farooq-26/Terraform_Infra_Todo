run-name: Multi-Stage-Infra-Deployment

on:
  push:
    branches:
      - main
      - 'releases/**'
permissions:
  id-token: write
  contents: read
jobs:
  Terraform-init-fmt-validate-plan:
    environment: dev

    name: terraform init fmt validate plan
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Hello Greeting
        run: echo "Hello world"


      - name: Azure Login
        uses: Azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Init
        working-directory: ./environments/dev
        run: terraform init -input=false -no-color
      
      - name: Terraform Format Check
        working-directory: ./environments/dev
        run: terraform fmt -check
      
      - name: Terraform Validate
        working-directory: ./environments/dev
        run: terraform validate
      
      - name: Terraform Plan
        working-directory: ./environments/dev
        run: terraform plan -var-file="terraform.tfvars" -no-color




   

  # scantrivi:
  #   name: Scantrivi
  #   runs-on: self-hosted

  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4

  #     - name: Scantrivi
  #       uses: scantrivi/scantrivi-action@v1
  #       with:
  #         scantrivi_token: ${{ secrets.SCANTRIVI_TOKEN }}
  #         scantrivi_project_id: 12345
