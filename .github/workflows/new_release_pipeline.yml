# run-name: DevSecOps Infra Pipeline - ${{ inputs.deploy_target }} by @${{ github.actor }}

# on:
#     push:
#       branches:
#         - main
#         - release/*
#     workflow_dispatch:
#       inputs:
#         deploy_target:
#           description: 'Select the deployment target environment'
#           required: true
#           default: 'dev'
#           type: choice
#           options:
#             - dev
#             - staging
#             - prod

# permissions:
#   contents: read
#   id-token: write
#   pull-requests: write
#   security-events: write

# env:
#   TF_working_dir: ./environments/dev
#   TF_version: 1.9.0
#   CHECKOV_version: 2.0.0

# jobs:
#   pre-checks:
#     name: Pre-scan checks
#     runs-on: self-hosted
    
#     steps:
#       - name: checkout code
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 0

#       - name: Gitleaks
#         # You may pin to the exact commit or the version.
#         # uses: gitleaks/gitleaks-action@e6dab246340401bf53eec993b8f05aebe80ac636
#         uses: gitleaks/gitleaks-action@v2.3.4

#       - name: Install gitleaks
#         shell: powershell
#         run: |
#           curl -sSL https://github.com/gitleaks/gitleaks/releases/download/v2.3.4/gitleaks_2.3.4_Linux_x86_64.tar.gz | tar -xz -C /usr/local/bin
        

#       - name: Run gitleaks scan
#         shell: powershell
#         run: |
#           gitleaks detect --source . --report-format json --report-path gitleaks_report.json

#       - name: Run TFLint
#         shell: powershell
#         run: |
#           tflint --init
#           tflint --format compact
#         continue-on-error: true
      
#   Terraform-plan:
#     name: Terraform Plan
#     runs-on: self-hosted
#     needs: pre-checks
#     steps:
#       - name: checkout code
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 0

#       - name: Azure Login
#         uses: azure/login@v1
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

#       - name: Terraform Init
#         working-directory: ${{ env.TF_working_dir }}
#         run: terraform init

#       - name: Terraform format check
#         working-directory: ${{ env.TF_working_dir }}
#         run: terraform fmt -check

#       - name: Terraform validate
#         working-directory: ${{ env.TF_working_dir }}
#         run: terraform validate

#       - name: Terraform Plan
#         working-directory: ${{ env.TF_working_dir }}
#         run: terraform plan 